#include "main.h"

/**
 * _isdigit - check if numbers ar 0 - 9
 * @c: char to be checked
 *
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if ((c >= 48)  (&& c <= 57))
	{
		return (1);
	}
		return (0);
}

